{
  "name": "my-real-plugin",
  "version": "1.0.0",
  "description": "A Figma plugin to save and manage other plugins",
  "main": "code.js",
  "scripts": {
    "build": "esbuild src/code.ts --bundle --outfile=code.js --loader:.html=text --platform=node",
    "watch": "npm run build -- --watch"
  },
  "author": "",
  "license": "MIT",
  "devDependencies": {
    "@figma/plugin-typings": "*",
    "esbuild": "^0.23.0",
    "typescript": "^4.0.0"
  }
}
